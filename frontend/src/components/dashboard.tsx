const Dashboard = () => {
  // const { user } = useContext(UserContext);
  return (
    <div>
      {/* <h1>Hi {user?.fullname}</h1> */}
      <h1>Welcome to meroDokan</h1>
    </div>
  );
};

export default Dashboard;
